💻 1. Core Development, Validation & Self-Repair Skills1. 
Context-Aware Compilation & Diagnostic Log Triager (Previously Suggested)What it is: A specialized tokenizing and error-mapping routine for system compilation.What it does: Runs project validation scripts (npx tsc --noEmit or python syntax linters), captures raw log buffers, isolates absolute file targets, and extracts error line coordinates.Why it's needed: Multi-page terminal stack traces confuse LLMs. This skill trims out noise, headers, and irrelevant warnings, handing the code repair loop a clean, single-point fix target.How an AI should build it: Deploy a background shell listener that captures stderr, pipes it through a regex tokenizer matching file-line patterns ([a-zA-Z0-9_/.-]+):(\d+):(\d+), and maps the first three distinct errors to a structural JSON payload.2. Absolute Path Traversal & Sandboxed Write Sentry (Previously Suggested)What it is: A defensive filesystem guard tracking directory layout updates.What it does: Intercepts path arguments from the agent, strips absolute prefixes and ../ loop segments, and verifies targets remain inside the active sandbox workspace.Why it's needed: Prevents a hallucinating model from writing code or scripts outside its assigned folder, completely protecting core platform assets like docs/AI_UPDATE_CHECKLIST.md from accidental corruption.How an AI should build it: Build a validation check using path.resolve() and path.relative(). If the calculated destination escapes the allowed project directory root, the file modifier crashes the step and logs a security exception.3. Automated Universal Version & Metadata Sync Guard (Previously Suggested)What it is: A project release-identity configuration script.What it does: Scans and verifies version fields across six primary file surfaces (src/version.ts, package.json, metadata.json, index.html, AGENTS.md, and MilestoneChecklist.tsx) before allowing task completion.Why it's needed: Enforces your system's strict version rules, completely eliminating human configuration errors and mismatch glitches across production surfaces.How an AI should build it: Write a script that treats src/version.ts as the single source of truth, parses the other five target files on disk, executes a strict string match verification, and hard-blocks the completion gate if a single field disagrees.4. Package Manager & Script Discovery AgentWhat it is: A local workspace runtime analyzer module.What it does: Scans new workspaces on mount to detect active lockfiles (package-lock.json, pnpm-lock.yaml, yarn.lock, requirements.txt) and reads available package scripts.Why it's needed: Allows the agent to automatically figure out if a project uses npm, pnpm, yarn, bun, or pip, choosing the correct validation tool without human guidance.How an AI should build it: Create a sequential directory file-check routine that populates an execution profile object with discovered available runtimes, script names, and lock versions.5. Git Feature Branch & PR Metadata PorterWhat it is: An automated source control workflow skill.What it does: Orchestrates localized git branches, stages files via clean porcelain commands, writes atomic commits, and compiles metadata for Pull Requests.Why it's needed: Isolates experimental agent modifications onto separate developer lanes, completely protecting your main repository tree from unstable code changes.How an AI should build it: Code a wrapper that triggers native git commands (git checkout -b, git commit -m) through child processes, checks for errors, and outputs clean JSON summaries containing diff statistics.6. Micro-Patch Tokenizer & AST Diff InspectorWhat it is: An abstract syntax tree (AST) code change auditor.What it does: Generates and reviews code diffs line-by-line; parses modified syntax structures to ensure the AI hasn't dropped required brackets, trailing commas, or parenthesis.Why it's needed: Standard line diffs fail to catch structural syntax breakages before build compilation steps. This skill flags unclosed scopes immediately.How an AI should build it: Create a script that passes modified files through a lightweight local language parser or generic brackets tokenizer, validating code structure symmetry before closing an edit turn.7. Dependency Matrix Lockfile AuditorWhat it is: A defensive third-party package security checker.What it does: Audits localized package files against an internal vulnerability manifest or local cache to detect outdated or broken packages.Why it's needed: Prevents the autonomous agent from importing deprecated, breaking, or insecure external components into your factory tree.How an AI should build it: Configure an isolated linter that scans the dependencies tree of a modified configuration file, cross-checking versions against a locked compatibility table.8. Shell Script Execution Sandbox SentryWhat it is: A terminal execution timeout and boundary fence.What it does: Spawns and monitors shell processes, enforces a strict timeout cutoff (max 60–120s), and isolates system environment handles.Why it's needed: Prevents runaway background processes or infinite loops (like a test suite that gets stuck waiting for user input) from locking up your host CPU cores.How an AI should build it: Build a node subprocess spawner with explicit timeout parameters, maxBuffer limits capped at 8MB, and an active kill-switch that sends SIGTERM if boundaries are crossed.9. Malformed JSON Stream Post-ProcessorWhat it is: A corrective string parser for local model outputs.What it does: Intercepts raw text returns, identifies JSON braces, strips away markdown backticks (```json), and re-escapes unescaped internal string quotation marks.Why it's needed: Local models occasionally append conversational padding or break escape structures when writing multi-line string payloads, causing execution crashes.How an AI should build it: Implement an regex matching envelope \{[\s\S]*\} that extracts the core object, validates it against standard token positions, and automatically repairs unescaped text content inside parameters.10. Orphaned Code File Sweep InspectorWhat it is: A source-tree cleanliness and hygiene manager.What it does: Scans target directories recursively, matches imported modules against existing files, and flags unreferenced, parallel, or duplicate files.Why it's needed: Satisfies your strict clean root rules by preventing abandoned file variants (like a duplicate Component1.tsx) from breaking compilation or misleading future agent passes.How an AI should build it: Code an indexing sweep that matches all active workspace files against imported file path strings, moving any unreferenced source files to a temporary .gina/archive location.