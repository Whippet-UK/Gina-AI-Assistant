🎬 Media, Video & Cinematic Processing Agents:
11. Video Tiling & Pixel-Headspace Budgeting Sentinel (Previously Suggested)What it is: A preemptive memory-footprint calculator for video generation pipelines.What it does: Reviews incoming user generation specs and locks spatial dimensions to a strict 393,216-pixel maximum ceiling.Why it's needed: Video models like Wan 2.1 allocate memory aggressively. This skill keeps direct text-to-video parameters within your hardware safety cage, preventing desktop driver crashes.How an AI should build it: Create a calculation script that matches requested clip durations with specific resolution caps (e.g., hard-locking at 512x512 if frames approach 73 limits).12. Multi-Platform Aspect Ratio Blurred Sidebar Porter (Previously Suggested)What it is: An automated video canvas layout adapter.What it does: Converts 16:9 landscape clips to vertical 9:16 portrait frames, duplicates the track to fill background empty spaces, box-blurs the background, and layers the original video centered on top.Why it's needed: Automates layout formatting for portrait/Shorts channels without requiring resource-heavy manual video editing.How an AI should build it: Build a headless multi-threaded FFmpeg pipeline mapping layout graphs with complex filter flags: [0:v]scale=1080:1920:force_original_aspect_ratio=increase,crop=1080:1920,boxblur=40:5[bg];[0:v]scale=1080:1920:force_original_aspect_ratio=decrease[fg];[bg][fg]overlay=(W-w)/2:(H-h)/2.13. CPU OpenCV Telea Watermark Inpainter (Previously Suggested)What it is: A model-free video watermark and logo removal utility.What it does: Targets user-configured corner percentages, generates a local binary mask, and applies frame-by-frame Telea pixel inpainting on the CPU.Why it's needed: Replaces VRAM-heavy neural logo removal with a lightweight, multi-threaded CPU pass, keeping your GPU memory clear for image and video generation.How an AI should build it: Code a Python script using pure OpenCV (cv2.VideoCapture and cv2.inpaint), scaling pixel masks to source frames using normalized coordinate rectangles.14. Wan 2.1 Direct Temporal Frame SegmenterWhat it is: An automated long-form video chunking manager.What it does: Divides extended narrative video timelines into isolated generation chunks of 3 seconds or less (max 73 frames at 24fps).Why it's needed: Enforces your hardware cage rules by preventing long generations from overwhelming VRAM, allowing long timelines to be generated piece-by-piece.How an AI should build it: Build an orchestration calculator that splits target lengths into distinct job items, tracking and applying the final frame of the previous clip as a seed for the next scene.15. Dynamic VFX Keyframe Timeline SchedulerWhat it is: A script-driven visual effects automation module.What it does: Generates keyframe parameters for your gina_motion_physics_engine.py pipeline, timing visual shakes and bloom filters to audio beats.Why it's needed: Simplifies video creation by automatically adjusting NumPy/OpenCV effect layers across a timeline without manual frame tracking.How an AI should build it: Map a time-step configuration array where intensity scalars for effect variables (like intensity, aberration_px, shake_amplitude) change based on a sine-wave function or a timestamp list.16. Green-Screen Chromakey Compositor SentryWhat it is: An automated edge-similarity video blending linter.What it does: Detects pure green channels (0x00FF00) across background clips and overlays them onto primary timeline items.Why it's needed: Standard chromakeying leaves harsh artifact borders on low-resolution videos. This skill cleans up edges based on frame-by-frame noise analysis.How an AI should build it: Pass overlay tracks through a fine-tuned FFmpeg chromakey filtering string, dynamically tuning similarity indices (chromakey=0x00FF00:0.15:0.1) depending on probed format data.17. Wan I2V Final-Frame Conditioning DirectorWhat it is: A cross-scene visual continuity manager.What it does: Extracts the terminal frame of a completed clip, writes it to a temporary input path, and injects it into a WanImageToVideo node for the next generation block.Why it's needed: Ensures smooth, logical visual continuity across multi-scene video stories instead of letting scene cuts jump chaotically.How an AI should build it: Create a script that hooks into final output files, runs an automated end-seeking FFmpeg extract ffmpeg -sseof -0.04 -i input.mp4 -frames:v 1 output.png, and automatically populates the LoadImage node of the subsequent queue item.18. ComfyUI Watchdog Hysteresis EvaluatorWhat it is: A failure-tolerant backend monitoring sentry.What it does: Tracks ComfyUI status checks, requiring at least two consecutive connection dropouts before marking the service offline.Why it's needed: Prevents temporary network drops or normal model-loading VRAM delays from flagging the system as offline and blocking the UI.How an AI should build it: Create an interval-based state counter. Increment a failure tracker on every rejected query, only flipping the global online status flag if the value crosses a strict threshold boundary.19. RIFE Motion Slow-Motion Target GovernorWhat it is: An automated frame-rate matching manager.What it does: Scans system stats; automatically changes video generation frames between 2x and 4x interpolation targets based on active GPU thermals.Why it's needed: Adjusts frame-rate generation workloads to keep system thermals below critical levels.How an AI should build it: Write an automated checker script that queries nvml telemetry. If core readings stay below 60°C, it activates the 4x interpolation lane; if heat rises, it drops down to 2x processing.20. Video Stream Codec & Color Channel NormalizerWhat it is: A pre-flight video file structure linter.What it does: Inspects video metadata, ensuring files match standard YUV420p color spaces and H.264 formats before processing.Why it's needed: Keeps the timeline concatenation tracker stable by catching incompatible container codecs that cause processing errors.How an AI should build it: Run an automated shell check using ffprobe -show_entries stream=pix_fmt,codec_name -of json, verifying that properties match expected profiles before passing clips to the video loop.