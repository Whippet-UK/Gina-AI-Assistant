🎵 Audio, Lyrics & Voice Master Agents:
21. Acoustic Block Crossfader & Overlap Matrix (Previously Suggested)What it is: A smooth audio block stitching module.What it does: Modifies the output of music_generator.py to calculate overlapping linear volume fades between sequential 30-second AudioCraft clips.Why it's needed: AudioCraft tracks are limited to 30-second windows. Without crossfading, splicing them end-to-end creates jarring jumps in volume and rhythm.How an AI should build it: Write a script using NumPy matrix functions that overlaps the last 1.5 seconds of a track segment with the beginning of the next, applying a math ramp to blend frequencies together smoothly.22. ACE-Step Local 8101 Port Router SentryWhat it is: A network binding tool for the local singing API.What it does: Checks active network bindings on boot, forces singing operations to port 8101, and cleans up old port allocations.Why it's needed: Ensures local singing tasks remain online by fixing port binding conflicts that can keep the service from starting.How an AI should build it: Write a script using PowerShell network commands (Get-NetTCPConnection -LocalPort 8101) to locate and stop old processes occupying the port, then launch the active API with clean path arguments.23. Lyrics Theme Condenser & Tag TokenizerWhat it is: A natural language text preprocessing module for audio models.What it does: Cleans and condenses raw lyric sheets into small, concise 300-character description tags optimized for local audio models.Why it's needed: Inpuiting overly long or messy text walls into audio models wastes context space and results in worse generation quality.How an AI should build it: Implement an isolation filter that extracts key emotional and stylistic tokens (like synthwave, dark energetic tempo, female vocals) from a song description, stripping out formatting symbols.24. PCM16 WAV Native Audio BuilderWhat it is: A backup audio file serialization utility.What it does: Saves raw audio tensor arrays directly as standard PCM-16 WAV files without relying on external system libraries.Why it's needed: Some Windows setups lack proper audio-saving backends. This provides a rock-solid, dependency-free file saver.How an AI should build it: Build a clean binary writer using Python's native wave and struct modules, translating floating-point tensors into signed 16-bit integer chunks.25. Mid-Side Audio Vocal Remover & SeparatorWhat it is: A phase-cancellation stem separation utility.What it does: Separates stereo tracks into center-panned frequencies (vocals/bass) and wide side channels (instruments) to create instrumentals.Why it's needed: Gives your system a quick, local audio isolation tool without needing heavy external neural separation libraries.How an AI should build it: Deploy a script using torchaudio that subtracts left and right audio channels from each other, applying a bandpass filter (200Hz to 4000Hz) to isolate vocal paths.26. Audio Continuation Chain LinkerWhat it is: An automated audio timeline extension supervisor.What it does: Extracts trailing audio segments from references and passes them to AudioCraft as continuity prompts.Why it's needed: Keeps the tempo, key, and instruments consistent when extending a short song into a longer track.How an AI should build it: Write a slicing module that captures the final 3 seconds of an existing track, resamples it to 32kHz, and sets it as the melody prompt for the next generation block.27. Audio Craft Library Cache EvaluatorWhat it is: An offline audio asset storage tracker.What it does: Monitors the models/audio sandbox folder, tracking safetensors versus raw binaries to prevent double size calculations.Why it's needed: Keeps your model inventory accurate by ensuring alternate weight layouts aren't counted twice in your dashboard.How an AI should build it: Code a file scanner using fs.statSync(), filtering for weight formats (.safetensors, .bin), picking the preferred version, and ignoring duplicate cache files.28. Automated Audio Deck Muxing PipelineWhat it is: An in-app audio library update pipeline.What it does: Takes completed tracks, writes them to your user upload folder, and automatically reloads the front-end player with the new track.Why it's needed: Lets you listen to your newly generated audio tracks instantly without needing to manually refresh the page or copy files.How an AI should build it: Create an Express API router hook that captures the output file path, registers the track in your local database, and dispatches a front-end event to load the new URL into the player.29. Lyrical Structure & Section Tag ValidatorWhat it is: A text formatting linter for song writing modules.What it does: Reviews generated text, ensuring it contains structural section tags (like [Verse 1], [Chorus], [Bridge]) before sending it to the singing engine.Why it's needed: Keeps the singing engine stable by catching unformatted lyric sheets that cause parsing errors during audio synthesis.How an AI should build it: Implement an inspection pass using regex rules to confirm the presence of required bracket tags, adding default section wrappers if they are missing.30. Dual-Weight Audio Profile Cache ExcluderWhat it is: A storage data-cleanup manager.What it does: Identifies alternate model formats (pytorch_model.bin vs model.safetensors) in the same folder and excludes the duplicate from disk count data.Why it's needed: Prevents your dashboard from displaying inflated, incorrect storage counts when a folder contains multiple model formats.How an AI should build it: Implement a directory scanner that groups files by model types, counts only the active preferred weights, and logs any redundant formats as hidden background files.